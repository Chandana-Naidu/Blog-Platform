<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registration - Dynamic Blogging</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@600&display=swap" rel="stylesheet">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600&display=swap');
        @import url('https://fonts.googleapis.com/css2?family=Oswald:wght@600&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body, html {
            height: 100%;
            font-family: 'Montserrat', sans-serif;
            background: linear-gradient(135deg, #FAD7A1 40%, #E96D71 100%);
            color: #333;
            overflow: hidden;
        }

        .container {
            position: relative;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .main-content {
            position: relative;
            width: 70%;
            text-align: center;
            z-index: 2;
        }

        h1, p {
            font-family: 'Oswald', sans-serif;
            font-size: 2.5rem;
            margin-bottom: 15px;
            color: #001F3F; /* Dark navy blue */
        }

        p {
            font-size: 1.2rem;
        }

        .cta-buttons {
            display: flex;
            gap: 15px;
            justify-content: center;
        }

        button {
            padding: 10px 30px;
            border: none;
            font-family: 'Montserrat', sans-serif;
            font-weight: bold;
            font-size: 1rem;
            cursor: pointer;
            border-radius: 30px;
            transition: all 0.3s ease;
        }

        .login-btn {
            background-color: #333;
            color: #FFF;
        }

        .register-btn {
            background-color: #FFF;
            color: #333;
        }

        button:hover {
            transform: translateY(-5px);
            box-shadow: 0px 10px 20px rgba(0, 0, 0, 0.2);
        }

        .floating-element {
            position: absolute;
            background-color: rgba(255, 255, 255, 0.1);
            width: 300px;
            height: 300px;
            top: -50px;
            right: -150px;
            border-radius: 50%;
            z-index: 1;
            pointer-events: none;
        }

        .registration-form {
            width: 400px;
            background-color: #FFF;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0px 10px 30px rgba(0, 0, 0, 0.2);
        }


        h1, p {
            font-family: 'Oswald', sans-serif;
            font-size: 2rem;
            margin-bottom: 15px;
            color: #001F3F; /* Dark navy blue */
            text-align: center;
        }

        p {
            font-size: 1rem;
        }

        label {
            display: block;
            margin-bottom: 15px;
            font-weight: 600;
        }

        input[type="text"],
        input[type="email"],
        input[type="password"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 8px;
            font-family: 'Montserrat', sans-serif;
        }

        .register-btn {
            display: block;
            width: 100%;
            padding: 10px;
            border: none;
            font-family: 'Montserrat', sans-serif;
            font-weight: bold;
            font-size: 1rem;
            cursor: pointer;
            border-radius: 30px;
            background-color: #001F3F; /* Dark navy blue */
            color: #FFF;
            transition: all 0.3s ease;
        }

        .register-btn:hover {
            background-color: #333;
            transform: translateY(-5px);
            box-shadow: 0px 10px 20px rgba(0, 0, 0, 0.2);
        }

    </style>
</head>

<body>
    <div class="container">
        <div class="registration-form">
            <h1>Login</h1>
            <p>Sign in to our Blogging community</p>
            <form action="/login" method="post">
                <label for="email">Email:</label>
                <input type="email" id="email" name="email" required>
                <label for="password">Password:</label>
                <input type="password" id="password" name="password" required>
                <input type="submit" value="Login" class="register-btn">
            </form>
        </div>
        <div class="floating-element"></div>
    </div>
</body>

</html>
